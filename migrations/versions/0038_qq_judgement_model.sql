-- QQ 全局判断模型（用户 2026-09-25）：判断开口兴趣打分用哪一个模型。
--
-- 此前这件事跟着绑定助手的对话模型走（`runtime.model_name`）。用户要求把它做成一份**QQ 全局**的设置
-- ——所有群与私聊共用同一个判断模型，不随当前助手切换——并放在「快捷管理 → 默认模型」页里选。它落在
-- `qq_settings`（单行表）上而不是方案里：判断用哪个模型是"这台机器怎么判断"的运行参数，不是某个方案的
-- 口味；方案已经用它自己的门槛与提示词表达口味了。
--
-- NULL = 跟随每间会话绑定助手的对话模型，也就是既有行为，所以**升级不回填**：老库升上来一个字都不变。
-- 列在表末尾（SQLite 的 ADD COLUMN 只能追加），`schema.ts` 的声明顺序刻意与迁移产物一致。
ALTER TABLE qq_settings ADD COLUMN judgement_model_name TEXT;
